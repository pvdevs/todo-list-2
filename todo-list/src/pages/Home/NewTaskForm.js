
export default function newTaskForm(taskTitle, taskDescription, taskDate, taskPriority, taskProject) {
    const formTitle = document.querySelector('.task-edit-title');
    const formDescription = document.querySelector('.task-edit-description');
    const formDate = document.querySelector('.task-edit-date');
    const formPriority = document.querySelector('.task-edit-priority');

    // Idk if this will be used
    const formCancelBtn = document.getElementById('task-edit-cancel-button');
    const formSaveBtn = document.getElementById('task-edit-save-button');

}